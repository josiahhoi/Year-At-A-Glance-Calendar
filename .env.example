# Google OAuth 2.0 Web Client ID (public — safe to commit the real one if you prefer).
# Create it in Google Cloud Console → APIs & Services → Credentials.
# See README.md for the full one-time setup walkthrough.
VITE_GOOGLE_CLIENT_ID=your-client-id.apps.googleusercontent.com
